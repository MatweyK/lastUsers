<?php

/**
 * Implements hook_theme().
 */
function users_theme($existing, $type, $theme, $path) {
  $variables = [
    'users' => [
      'variables' => [
        'source_text' => NULL,
      ],
      'template' => 'users',
    ]
  ];
  return $variables;
}
